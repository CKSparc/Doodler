<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/action_delete"
        android:icon="@android:drawable/ic_menu_close_clear_cancel"
        android:title="action_delete"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_undo"
        android:icon="@drawable/undoarrow"
        android:title="action_undo"
        app:showAsAction="always"/>

    <item
        android:id="@+id/action_redo"
        android:icon="@drawable/redoarrow"
        android:title="action_redo"
        app:showAsAction="always"/>

    <!-- Settings, should always be in the overflow -->
    <item
        android:id="@+id/action_edit_size"
        android:title="Edit Brush Size"
        app:showAsAction="never"/>

    <item android:id="@+id/action_settings"
        android:title="Edit Opacity"
        android:orderInCategory="100"
        app:showAsAction="never"/>

</menu>